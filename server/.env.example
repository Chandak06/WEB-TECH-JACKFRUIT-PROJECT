# Server Configuration
PORT=4000

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=comeback

# CORS allowed origin
CLIENT_ORIGIN=http://localhost:5173

# Storage type: set to true to use MongoDB, false for file-based
USE_MONGO=false

# MongoDB Configuration (only needed if USE_MONGO=true)
# SERVERLINK=mongodb+srv://username:password@cluster.mongodb.net/
# DB_NAME=skillswap
